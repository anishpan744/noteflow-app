import 'package:flutter/material.dart';

// ── Background hierarchy ──────────────────────────────────────────────────────
const kBg0 = Color(0xFF050810); // deepest — page background
const kBg1 = Color(0xFF080D1A); // surface — cards, drawers
const kBg2 = Color(0xFF0D1424); // raised — input fields, list items
const kBg3 = Color(0xFF141B2E); // overlay — tooltips, menus

// ── Accent / data colors ──────────────────────────────────────────────────────
const kNeon    = Color(0xFF00D4FF); // primary — electric blue
const kViolet  = Color(0xFF7B61FF); // tasks module
const kTeal    = Color(0xFF00FFB3); // success / done
const kAmber   = Color(0xFFFFB800); // warning / priority
const kCrimson = Color(0xFFFF3366); // danger / overdue / delete

// ── Glass card ────────────────────────────────────────────────────────────────
const kGlassFill   = Color(0x1AFFFFFF); // 10% white fill
const kGlassBorder = Color(0x33FFFFFF); // 20% white border
const kGlowBlue    = Color(0x2600D4FF); // 15% blue inner glow

// ── Text ──────────────────────────────────────────────────────────────────────
const kTextPrimary   = Color(0xFFF0F4FF);
const kTextSecondary = Color(0xFF8892AA);
const kTextMuted     = Color(0xFF4A5568);

// ── Arctic light theme ────────────────────────────────────────────────────────
const kLightBg0  = Color(0xFFF0F4FF);
const kLightBg1  = Color(0xFFE8EEF8);
const kLightBg2  = Color(0xFFDDE5F5);
const kLightBg3  = Color(0xFFD0DAEE);
const kNeonLight = Color(0xFF0066CC); // darkened for legibility on light

const kLightTextPrimary   = Color(0xFF0A0F1E);
const kLightTextSecondary = Color(0xFF3A4560);
const kLightTextMuted     = Color(0xFF7A8AA8);
